diff --git a/megatron/core/dist_checkpointing/strategies/nvrx.py b/megatron/core/dist_checkpointing/strategies/nvrx.py
index e4b1c1a9a..8986fb611 100644
--- a/megatron/core/dist_checkpointing/strategies/nvrx.py
+++ b/megatron/core/dist_checkpointing/strategies/nvrx.py
@@ -41,9 +41,8 @@ def has_nvrx_async_support() -> bool:
         getattr(state_dict_saver, "save_state_dict_async_finalize", None),
         getattr(state_dict_saver, "save_state_dict_async_plan", None),
     )
-    assert (
-        is_nvrx_min_version()
-    ), f"Minimum required nvidia-resiliency-ext package version is {NVRX_MIN_VERSION}."
+    if not is_nvrx_min_version():
+        return False
 
     return all(symbol is not None for symbol in required_symbols) and hasattr(
         filesystem_async, "_results_queue"
diff --git a/megatron/core/transformer/attention.py b/megatron/core/transformer/attention.py
index b27f90c53..534668771 100644
--- a/megatron/core/transformer/attention.py
+++ b/megatron/core/transformer/attention.py
@@ -259,6 +259,8 @@ class SelfAttentionSubmodules:
     linear_proj: LinearProjBuilder
     q_layernorm: LayerNormBuilder | None = None
     k_layernorm: LayerNormBuilder | None = None
+    # OV2.1 patch: optional custom rotary apply fn (e.g. apply_rotary_pos_emb_vision).
+    apply_rotary_fn: Optional[Callable] = None
 
 
 @dataclass
@@ -1225,10 +1227,13 @@ class Attention(MegatronModule, ABC):
                 cu_seqlens_q = cu_seqlens_kv = None
 
             if split_qkv:
+                # OV2.1 patch: dispatch to a custom rotary apply fn if the spec set one
+                # (e.g. apply_rotary_pos_emb_vision); else use the default mcore impl.
+                _rot_fn = getattr(self, "apply_rotary_fn", None) or apply_rotary_pos_emb
                 if q_pos_emb is not None:
                     # TODO VIJAY: simplify
                     if inference_context is None or inference_context.is_static_batching():
-                        query = apply_rotary_pos_emb(
+                        query = _rot_fn(
                             query,
                             q_pos_emb,
                             config=self.config,
@@ -1241,7 +1246,7 @@ class Attention(MegatronModule, ABC):
                             query, q_pos_emb, self.config, cu_seqlens_q, self.pg_collection.cp
                         )
                 if k_pos_emb is not None:
-                    key = apply_rotary_pos_emb(
+                    key = _rot_fn(
                         key,
                         k_pos_emb,
                         config=self.config,
@@ -1403,6 +1408,9 @@ class SelfAttention(Attention):
             name=name,
         )
 
+        # OV2.1 patch: optional custom rotary apply fn (e.g. apply_rotary_pos_emb_vision).
+        self.apply_rotary_fn = getattr(submodules, "apply_rotary_fn", None)
+
         self.linear_qkv_out_dim = self.query_projection_size + 2 * self.kv_projection_size
         if self.config.attention_output_gate:
             self.linear_qkv_out_dim += self.config.kv_channels * self.config.num_attention_heads
